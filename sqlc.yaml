version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/*.sql"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        overrides:
          - db_type: "decimal"
            go_type: "float64"