app:
  name: "gofiber-skeleton"
  env: "development"
  port:
    http: 8080
    https: 8443
  path:
    cert: "./internal/assets/tls/zercle-dev.crt"
    priv: "./internal/assets/tls/zercle-dev.key"
    ca: "./internal/assets/tls/rootCA.crt"
    log: "./log"

db:
  main:
    sock: "/var/run/mysqld/mysqld.sock"
    host: "localhost"
    port: 3306
    username: ""
    password: ""
    db_name: ""
  redis:
    host: "localhost"
    port: 6379
    username: ""
    password: ""
    db_name: 0

jwt:
  # openssl ecparam -name prime256v1 -genkey -noout -out privkey.pem
  private: "./internal/assets/jwt/dev/privkey.pem"
  # openssl ec -in privkey.pem -pubout -out pubkey.pem
  public: "./internal/assets/jwt/dev/pubkey.pem"